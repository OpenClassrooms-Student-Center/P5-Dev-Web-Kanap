let basket = JSON.parse(localStorage.getItem('basket'));
console.log(basket);

for (i in basket) {
  let basket = basket[i];
  let productId = product['_id'];
  let productCouleur = product['couleur'];
  let productQuantite = product['quantite'];
}
